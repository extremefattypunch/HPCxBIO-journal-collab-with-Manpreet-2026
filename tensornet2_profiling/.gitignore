# Profiling artifacts (large / machine-specific) — keep the scripts, not the outputs.
results/*.nsys-rep
results/*.sqlite
results/*.ncu-rep
results/*.csv
results/*.json
results/*.qdrep
__pycache__/
*.pyc
# datasets downloaded at runtime
data/
*.npz
# cloned dependency (editable install target) — large, has its own git history
third_party/
